const Base = () => {
    return <div style = {
        { textAlign: 'center' } } > A site where people can check the recipe of different
    variety of food, < br / > check their body mass index and whether that suggest to normality or not, < br / >
        Find four different ideal weight scores according to four different well - known equations < br / >
        Calculate the burned calorie
    for a specific activity. < /div>;
};

export default Base;